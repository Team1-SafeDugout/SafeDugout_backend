<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="adminNews">

<!-- 	private int postNumber; // 게시글 번호
	private String postTitle; // 게시글 제목
	private String postContent; // 게시글 내용
	private String postDate; // 작성일자
	private int teamNumber; // 팀 번호
	private String journalist; // 기자명
	private String postLink; // 기사원문 링크	
	private int adminNumber; // 어드민 번호
	private int postType; // 보드 번호
	private List<AttachmentDTO> attachment; 
	private String imgPath;
	private String teamName; -->

	<!-- 뉴스 목록 조회 -->
	<select id="listSelect" resultType="NewsPostDTO" parameterType = "map">
	    SELECT *
	    FROM (
	        SELECT ROWNUM AS rnum, subquery.*
	        FROM (
	            SELECT POST_NUMBER,
	                   POST_TITLE,
	                   POST_CONTENT,
	                   POST_DATE,
	                   TEAM_NUMBER,
	                   JOURNALIST,
	                   POST_LINK,
	                   ADMIN_NUMBER
	            FROM TBL_POST
	            WHERE POST_TYPE = 3
	            ORDER BY POST_DATE DESC
	        ) subquery
	    )
	    WHERE rnum BETWEEN #{startRow} AND #{endRow}
	</select>
	
	<!-- 뉴스 상세 조회 -->
	<select id="detailSelect" parameterType="int" resultType="NewsPostDTO">
	    SELECT p.POST_NUMBER,
	           p.POST_TITLE,
	           p.POST_CONTENT,
	           p.POST_DATE,
	           p.TEAM_NUMBER,
	           p.JOURNALIST,
	           p.POST_LINK
	    FROM TBL_POST p
	    WHERE p.POST_NUMBER = #{postNumber}
	</select>
	
	<select id = "selectAll" resultType = "NewsPostDTO">
	    SELECT p.POST_NUMBER,
	           p.POST_TITLE,
	           p.POST_CONTENT,
	           p.POST_DATE,
	           p.TEAM_NUMBER,
	           p.JOURNALIST,
	           p.POST_LINK
	    FROM TBL_POST p
	    WHERE POST_TYPE = 3
	</select>
	
	<select id = "total" resultType = "int">
	SELECT COUNT(POST_TYPE)
	FROM TBL_POST
	WHERE POST_TYPE = 3
	</select>

	
	<!-- 뉴스 추가 -->
	<insert id="insert">
		<selectKey keyProperty="postNumber" resultType="int" order="BEFORE">
			SELECT SEQ_POST.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO TBL_POST (POST_NUMBER, TEAM_NUMBER, ADMIN_NUMBER, POST_TYPE, POST_TITLE,POST_CONTENT, POST_DATE, POST_LINK, JOURNALIST)
		VALUES(seq_post.nextval, #{teamNumber}, #{adminNumber}, #{postType}, #{postTitle},#{postContent}, sysdate, #{postLink} , #{journalist})
	</insert>
	
	<!-- 뉴스 삭제 -->
	<delete id="delete" parameterType="int">
		DELETE 
		FROM TBL_POST
		WHERE POST_NUMBER = #{postNumber}
	</delete>
	
</mapper>