<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="adminNews">

	<!-- 뉴스 목록 조회 -->
	<select id="listSelect" resultType="NewsPostDTO">
	    SELECT *
	    FROM (
	        SELECT ROWNUM AS rnum, subquery.*
	        FROM (
	            SELECT POST_NUMBER,
	                   POST_TITLE,
	                   POST_CONTENT,
	                   POST_DATE,
	                   TEAM_NUMBER,
	                   JOURNALIST
	            FROM TBL_POST
	            WHERE BOARD_ID = 3
	            AND TEAM_NUMBER = #{teamNumber}
	            ORDER BY POST_DATE DESC
	        ) subquery
	    )
	    WHERE rnum BETWEEN #{startRow} AND #{endRow};
	</select>
	
	<!-- 뉴스 상세 조회 -->
	<select id="detailSelect" parameterType="int" resultType="NewsPostDTO">
	    SELECT p.POST_NUMBER,
	           p.POST_TITLE,
	           p.POST_CONTENT,
	           p.POST_DATE,
	           p.TEAM_NUMBER,
	           p.JOURNALIST,
	           p.POST_LINK,
	           a.ATTACHMENT_NUMBER,
	           A.ATTACHMENT_NAME,
	           A.ATTACHMENT_PATH
	    FROM TBL_POST p
	    LEFT JOIN TBL_POST_ATTACHMENT pa ON p.POST_NUMBER = pa.POST_NUMBER
	    LEFT JOIN TBL_ATTACHMENT a ON pa.ATTACHMENT_NUMBER = a.ATTACHMENT_NUMBER
	    WHERE p.POST_NUMBER = #{postNumber};
	</select>
	
	<select id = "selectAll" resultType = "NewsPostDTO">
	    SELECT p.POST_NUMBER,
	           p.POST_TITLE,
	           p.POST_CONTENT,
	           p.POST_DATE,
	           p.TEAM_NUMBER,
	           p.JOURNALIST,
	           p.POST_LINK,
	           a.ATTACHMENT_NUMBER,
	           A.ATTACHMENT_NAME,
	           A.ATTACHMENT_PATH
	    FROM TBL_POST p
	    LEFT JOIN TBL_POST_ATTACHMENT pa ON p.POST_NUMBER = pa.POST_NUMBER
	    LEFT JOIN TBL_ATTACHMENT a ON pa.ATTACHMENT_NUMBER = a.ATTACHMENT_NUMBER
	    WHERE BOARD_ID = 3
	</select>
	
	<select id = "total" resultType = "int">
	SELECT COUNT(BOARD_ID)
	FROM TBL_POST
	WHERE BOARD_ID = 2
	</select>

	
	<!-- 뉴스 추가 -->
	<insert id="insert">
		<selectKey keyProperty="postNumber" resultType="int" order="BEFORE">
			SELECT SEQ_POST.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO TBL_POST (POST_NUMBER, TEAM_NUMBER, ADMIN_NUMBER, BOARD_ID, POST_TITLE, POST_DATE, POST_LINK,POST_CONTENT, JOURNALIST)
		VALUES(#{postNumber}, #{teamNumber}, #{adminNumber}, #{boardId}, #{postTilte}, #{postDate}, #{postLink} , #{journalist})
	</insert>
	
	<!-- 뉴스 삭제 -->
	<delete id="delete" parameterType="int">
		DELETE 
		FROM TBL_POST
		WHERE POST_NUMBER = #{postNumber}
  		AND TEAM_NUMBER = #{teamNumber}
  		AND BOARD_ID = #{boardId}
	</delete>
	
</mapper>