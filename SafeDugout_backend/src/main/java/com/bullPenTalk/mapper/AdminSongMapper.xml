<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="adminSong">

	<!-- 팀 선수 응원가 목록 조회 -->
	<select id="listSelectAll" resultType="SongDTO">
	    SELECT *
	    FROM (
	        SELECT ROWNUM AS rnum, subquery.*
	        FROM (
	            SELECT POST_NUMBER,
	                   POST_TITLE,
	                   POST_DATE,
	                   TEAM_NUMBER,
	                   BOARD_TYPE_ID,
	                   POST_LINK,
	                   ADMIN_ID,
	                   POST_CONTENT
	            FROM TBL_POST
	            WHERE BOARD_TYPE_ID IN (5, 6)
	              AND TEAM_NUMBER = #{teamNumber}
	            ORDER BY POST_DATE DESC
	        )subquery
	    )
	    WHERE rnum BETWEEN #{startRow} AND #{endRow};
	</select>

	<!-- 팀응원가 목록 조회 -->
		<select id="listSelectTeam" resultType="SongDTO">
	    SELECT *
	    FROM (
	        SELECT ROWNUM AS rnum, subquery.*
	        FROM (
	            SELECT POST_NUMBER,
	                   POST_TITLE,
	                   POST_DATE,
	                   TEAM_NUMBER,
	                   BOARD_TYPE_ID,
	                   ADMIN_ID
	            FROM TBL_POST
	            WHERE BOARD_TYPE_ID = 5
	              AND TEAM_NUMBER = #{teamNumber}
	            ORDER BY POST_DATE DESC
	        )subquery
	    )
	    WHERE rnum BETWEEN #{startRow} AND #{endRow};
	</select>
	
	<!-- 선수 응원가 목록 조회 -->
	<select id="listSelectPlayer" resultType="SongDTO">
	    SELECT *
	    FROM (
	        SELECT ROWNUM AS rnum, subquery.*
	        FROM (
	            SELECT POST_NUMBER,
	                   POST_TITLE,
	                   POST_DATE,
	                   TEAM_NUMBER,
	                   BOARD_TYPE_ID,
	                   ADMIN_ID
	            FROM TBL_POST
	            WHERE BOARD_TYPE_ID = 6
	              AND TEAM_NUMBER = #{teamNumber}
	            ORDER BY POST_DATE DESC
	        )subquery
	    )
	    WHERE rnum BETWEEN #{startRow} AND #{endRow};
	</select>
	
	<!-- 팀 응원가 추가 -->
	<insert id="teamInsert">
		<selectKey keyProperty="postNumber" resultType="int" order="BEFORE">
			SELECT SEQ_POST.CURRVAL FROM DUAL
		</selectKey>
		INSERT INTO TBL_POST (POST_NUMBER, TEAM_NUMBER, ADMIN_NUMBER, BOARD_ID, POST_TITLE, POST_DATE, POST_LINK, POST_CONTENT)
		VALUES(SEQ_POST.NEXTVAL, #{teamNumber}, #{adminNumber}, #{boardId}, #{postTilte}, #{postDate}, #{postLink}, #{postContent})
	</insert>
	
	<!-- 선수 응원가 추가 -->
	<insert id="palyerInsert">
		<selectKey keyProperty="postNumber" resultType="int" order="BEFORE">
			SELECT SEQ_POST.CURRVAL FROM DUAL
		</selectKey>
		INSERT INTO TBL_POST (POST_NUMBER, TEAM_NUMBER, ADMIN_NUMBER, BOARD_ID, POST_TITLE, POST_DATE, POST_LINK)
		VALUES(SEQ_POST.NEXTVAL, #{teamNumber}, #{adminNumber}, #{boardId}, #{postTilte}, #{postDate}, #{postLink})
	</insert>
	
	<!-- 응원가 삭제 -->
	<delete id="delete" parameterType="int">
		DELETE 
		FROM TBL_POST
		WHERE POST_NUMBER = #{postNumber}
  		AND TEAM_NUMBER = #{teamNumber}
  		AND BOARD_ID = #{boardId}
	</delete>
	
		
</mapper>